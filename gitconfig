[user]
	name = Marco Westerhof
	email = westerhof.marco@gmail.com

[color]
    ui = true
    status = auto
    log = auto
    diff = auto

[alias]
    st = status
    ci = commit
    co = checkout
    rekt = "log --graph --all --oneline --decorate"
    gud = "log --graph --all --pretty=format:'%C(red)%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cD) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative"
[core]
	editor = vim
[push]
	default = simple
[pull]
	rebase = true
