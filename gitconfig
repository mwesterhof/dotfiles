[user]
    name = "Marco Westerhof"
    email = "m.westerhof@lukkien.com"

[color]
    ui = true
    status = always
    log = always
    diff = always

[alias]
    st = status
    ci = commit
    co = checkout
    rekt = "log --graph --all --oneline --decorate"
[core]
	editor = vim
[push]
	default = simple
